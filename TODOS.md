
# Todos

- Get it to work on more recent Windows 11 versions
- There should be no change of the top window when clicking on the desktop while coming from another one
- Clicking on windows in Icons render mode doesn't work reliably when an application has more than one window
- there should be a way to make the application temporarily transparent (e.g. when hovered)
- Fix render issue when there is an empty virtual desktop in the middle
- Potential crash when the render target is gone (can happen on remote desktops)
- Add tooltips / infos in the settings dialog
 